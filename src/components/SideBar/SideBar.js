import React from 'react';
import './SideBar.css';

const SideBar = () => {
    return (
        <div className='side-bar'>
            this is side bar
        </div>
    );
};

export default SideBar;